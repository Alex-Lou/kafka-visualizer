spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kafka_visualizer?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Mechanique.2022

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
        show_sql: false

  kafka:
    bootstrap-servers: localhost:9092

  mail:
    host: smtp.gmail.com
    port: 587
    username: alex.guennad@gmail.com
    password: bbrwnmkheyjnrfva
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

cors:
  allowed-origins: http://localhost:3000,http://localhost:5173,chrome-extension://*

app:
  jwt-secret: dev-secret-key-not-for-production-use-only-12345678901234567890
  kafka:
    message-retention-hours: 24
    max-messages-per-topic: 10000

logging:
  level:
    root: INFO
    com.kafkaflow.visualizer: INFO
    com.zaxxer.hikari: WARN
    org.hibernate: WARN
